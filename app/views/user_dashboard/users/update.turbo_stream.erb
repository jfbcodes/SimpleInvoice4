<%= turbo_stream.replace "user__#{@user.id}" do %>
  <%= render partial: 'line', locals: {user: @user  } %>
<% end %>

<%= turbo_stream.replace "flash_notices" do %>
  <%= render partial: "layouts/flash_notices" %>
  <% if @user.errors.any? %>
  <%= render partial: "errors", locals: {resource: @user} %>
  <% end %>
<% end %>
